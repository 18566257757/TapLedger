# 在 iPhone 安装 TapLedger PWA

1. 完成 Cloudflare 部署并在 Safari 打开自己的 HTTPS Worker URL。
2. 登录并确认 Home、Transactions 和 Settings 可用。
3. Safari 分享 → 添加到主屏幕，名称保留为 TapLedger。
4. 从主屏幕打开，确认底部导航、主题与交易编辑器正常。

离线时 App Shell 可打开；手工新增进入当前浏览器的明确待同步队列，不会后台静默写账。清除 Safari 网站数据前先恢复网络并确认队列已经同步。
